server: 
  port: 8086
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ecrisostomop/P2.config-service.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
      
#Colocar dependencia starter bootstrap#
